# Stage 2 FreeRADIUS client example.
#
# Replace every placeholder locally. Never commit the replacement secret.

client stage2-ap-gateway {
    ipaddr = <AP_GATEWAY_IP>
    secret = <RADIUS_SECRET>
    shortname = stage2-ap
    nastype = other
    require_message_authenticator = yes
}
